// Manipulando Strings e Números

// Contar quantos caracteres tem uma palavra e quantos digitos tem um número.
let word = "paralelepipedo"
console.log(word.length) // mostra quantos caracteres tem a string
let number = 1234
console.log(String(number).length) // para contar a auqntidade de digitos, primeiro converto number para string.